{
  "name": "editor",
  "version": "0.0.0",
  "devDependencies": {
    "generator-flight": "~0.2.1",
    "karma": "~0.8.5"
  },
  "scripts": {
    "test": "./node_modules/.bin/karma start --single-run --browsers PhantomJS"
  }
}
